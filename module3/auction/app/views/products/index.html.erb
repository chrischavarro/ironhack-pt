<h1>Welcome to BuySomeShit.com </h1>

<ul>
	<% @products.each do |product| %>
	<li>
		<%= link_to(product.title, product_path(product)) %>
	</li>
	<% end %>
</ul>

<%= link_to("Add a new Product", new_product_path) %>